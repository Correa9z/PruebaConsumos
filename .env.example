# Despliegue Docker / EC2
# Copiar a .env en la raíz del proyecto y completar. No subir .env al repositorio.
# En EC2: usar la IP pública o dominio (ej. http://3.80.xxx.xxx o https://tudominio.com)

# Base de datos (en Docker se sobrescribe con host 'postgres'; aquí por si corres solo la API)
DATABASE_URL="postgresql://prueba:prueba@postgres:5432/prueba"

# URLs públicas: en EC2 poner la URL por la que accederán (IP o dominio)
FRONTEND_URL=http://TU_IP_O_DOMINIO
API_URL=http://TU_IP_O_DOMINIO

# Redirect del proveedor de pagos (URL pública del backend para callbacks)
PAYMENT_REDIRECT_URL=

# Integración de pagos (obtener llaves en el dashboard del proveedor)
# Base URL: según documentación del proveedor (sandbox / producción)
PAYMENT_BASE_URL=
PAYMENT_PUBLIC_KEY=
PAYMENT_PRIVATE_KEY=
PAYMENT_INTEGRITY_KEY=
PAYMENT_EVENTS_SECRET=
